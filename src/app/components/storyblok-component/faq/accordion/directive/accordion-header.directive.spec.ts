import { AccordionHeader } from './accordion-header.directive';

describe('AccordionHeaderDirective', () => {
  it('should create an instance', () => {
    // const directive = new AccordionHeader();
    // expect(directive).toBeTruthy();
  });
});
